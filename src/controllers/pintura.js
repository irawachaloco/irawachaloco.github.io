(function (angular) {
    'use strict';

    angular.module('wPage').controller('pintura', [
        '$scope',
        function ($scope) {
            $scope.name = 'My Name';
        }
    ]);
}(angular));